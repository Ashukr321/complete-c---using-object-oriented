// volume of shpere in the cpp 
#include<iostream>
using namespace std;

// main method start here 
int main(){
    double r , volume ;
    cout<<"enter the radius of shpere "<<endl;
    cin>>r;
    volume = (4.0/3)*(22.0/7)*(r)*(r)*(r);
    cout<<"volume of the sphare is : "<<volume<<endl;
    system("pause");
    return 0; 
}